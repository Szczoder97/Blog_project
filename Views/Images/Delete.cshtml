@model Blog_project.Models.Image

@{
    ViewData["Title"] = "Delete";
}


<section class="section">
    <div class="container pb-6 mb-6">
        <div class="column is-3"></div>
        <div class="column is-7 mb-6 pb-6">
            <div class="container">
                <div class="mb-3">
                    <h1 class="title">Delete</h1>
                    <h2 class="subtitle">Are you sure you want to delete this?</h2>
                </div>
                    @*<dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Title)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Title)
                    </dd>
                    <dt class="col-sm-2">
                        Uploaded image
                    </dt>*@
                    <dd class="image mb-3">
                        <img src="@("~/StoredImages/" + Model.Title)" asp-append-version="true" width="250px" height="250px" />
                    </dd>

                <form asp-action="Delete">
                    <nav class="level form-group">
                        <div class="level-left">
                            <input type="hidden" asp-for="ImageId" />
                            <input type="submit" value="Delete" class="button is-danger" />
                        </div>
                        <div class="level-right">
                            <a asp-action="Index" class="button level-right">Back to List</a>
                        </div>
                    </nav>
                </form>
            </div>
        </div>
    </div>
</section>

